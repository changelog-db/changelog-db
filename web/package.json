{
  "name": "web",
  "version": "0.0.1",
  "packageManager": "pnpm@8.4.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "qwik build",
    "build.client": "vite build",
    "build.preview": "vite build --ssr src/entry.preview.tsx",
    "build.server": "vite build -c adapters/static/vite.config.ts",
    "build.types": "",
    "qwik": "qwik"
  },
  "devDependencies": {
    "@types/node": "^20.1.4",
    "prettier": "2.8.8"
  },
  "//": "This is a static app with zero runtime dependencies, so I'm treating build dependencies as normal dependencies. I don't need to install prettier on Netlify.",
  "dependencies": {
    "@builder.io/qwik": "^1.1.2",
    "@builder.io/qwik-city": "^1.1.2",
    "daisyui": "^2.51.6",
    "sirv-cli": "^2.0.2",
    "tailwindcss": "^3.3.2",
    "typescript": "5.0.4",
    "vite": "4.3.5",
    "vite-tsconfig-paths": "4.2.0",
    "@rollup/plugin-yaml": "^4.1.0"
  }
}
